<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-feature android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:theme="@style/Theme.AppCompat"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".ticket"/>
        <activity android:name=".PedestrianSummary" />
        <activity android:name=".PedestrianDetails" />
        <activity android:name=".DriverSummary" />
        <activity android:name=".DriverDetails" />

        <activity android:name=".Violations" />
        <activity android:name=".Location" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".Views"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="ocr, face, barcode, label"/>

    </application>

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>